// import { atomWithStorage } from 'jotai/utils';

import { atom } from 'jotai';

// export const playerTeamAtom = atomWithStorage('player', null);

// export const computerTeamAtom = atomWithStorage('computer', null);

export const playerTeamAtom = atom(null);

export const computerTeamAtom = atom(null);
